import { Component } from '@angular/core';

@Component({
  selector: 'app-ocorrencia',
  templateUrl: './ocorrencia.component.ts',
  styleUrls: ['ocorrencia.component.scss'],
})
export class OcorrenciaPage {

  constructor() {}

  // campos ntitulo TEXT NOT NULL, numero_ap integer, bloco TEXT, local TEXT, descricao TEXT NOT NULL, status integer NOT NULL, usuarios_id

}
